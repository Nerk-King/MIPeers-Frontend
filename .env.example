# Optional application backend endpoint. Never put private RAG credentials in VITE variables.
VITE_RAG_ENDPOINT=

# Optional authentication endpoint. When unset, login runs in demo mode against a dummy check.
VITE_AUTH_ENDPOINT=
